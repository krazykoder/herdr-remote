{
  "name": "herdr-remote-tests",
  "private": true,
  "description": "Test manifest and build scripts for modular frontend.",
  "scripts": {
    "build": "python3 scripts/build.py",
    "test": "node --test tests/*.js",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
