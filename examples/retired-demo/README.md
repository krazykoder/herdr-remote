# Retired demo

These are the original public-demo sources, retained only as historical examples.

- They are not imported, tested, deployed, or linked by the product.
- `telegram_demo.py` contains the former public relay URL. Do not run it or trust that endpoint.
- Recreate a demo only with infrastructure you own and a separately chosen URL.

