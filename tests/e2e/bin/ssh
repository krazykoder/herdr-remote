#!/usr/bin/env python3
"""Fake ssh: strips -o flags, treats the next arg as the host, execs the rest locally."""
import os, sys

args = sys.argv[1:]
while args and args[0] == "-o":
    args = args[2:]
host, cmd = args[0], args[1:]
os.environ["HERDR_FAKE_HOST"] = host
os.execv(cmd[0], cmd)
